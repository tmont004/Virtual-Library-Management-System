#to use this
#add a .txt to the end of the other makefile or change the name, so it wont interfere with this one or even delete it

#change this file name to 'Makefile' no extensions
#go to the folder where all the files are, including this one (on terminal)
#type 'make'
#then type 'my_program.exe'
#it will compile everything and run it
#if it works just delete this comment section



# compiler
CXX = g++
CXXFLAGS = -std=c++11 -Wall -I.

# .cpp are the source files and the .obj are compiled object files pre-linked
SOURCES = BinarySearchTree.cpp Book.cpp bookdatabase.cpp LibrarySystem.cpp main.cpp security.cpp user_system.cpp
OBJECTS = $(SOURCES:.cpp=.obj)

# this is the name of the final executable, so you type my_program.exe to run the program
TARGET = my_program.exe

# Default rule, idk but it's necessary
all: $(TARGET)

# Rule to link object files to create the final executable
$(TARGET): $(OBJECTS)
	$(CXX) $(CXXFLAGS) -o $(TARGET) $(OBJECTS)

# Rule to compile source files into object files
%.obj: %.cpp
	$(CXX) $(CXXFLAGS) -c $< -o $@

# Clean rule to remove generated files
clean:
	del $(TARGET) $(OBJECTS)

# Phony targets, no idea what this means
.PHONY: all clean
